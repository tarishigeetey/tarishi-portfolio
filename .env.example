# Copy to .env for local testing of the live bot, and set the same variable
# in Vercel → Project → Settings → Environment Variables for production.
#
# The bot is FREE with Groq (no credit card): https://console.groq.com/keys
GROQ_API_KEY=

# Optional — only if you switch the bot to Google Gemini (see api/chat.js note).
# GEMINI_API_KEY=
